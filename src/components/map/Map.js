import React from 'react';
import styled from 'styled-components';

import {
	StyledMap
} from './Map.styles';

const Map = () => {
	return (
		<StyledMap />
	);
};

Map.propTypes = {
};

Map.defaultProps = {
};

export default Map;
